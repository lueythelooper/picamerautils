#!/bin/bash

if command -v rpicam-vid &> /dev/null; then
    echo ""
else
    echo "ERROR: Install rpicam-vid on this machine"
    exit -1
fi

WIDTH=1920
HEIGHT=1080
FRAMERATE=15

python3 /usr/share/picamproject/flask_server.py $WIDTH $HEIGHT $FRAMERATE
